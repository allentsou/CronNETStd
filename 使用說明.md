# CronNETStd 使用說明

> 輕量分鐘級 cron 排程器。Target：`netstandard2.0` / `netstandard2.1` / `net8.0` / `net9.0`
> （可被 .NET Framework 4.7.2+、.NET Core 2.0+、.NET 5+ 引用）

## 1. 核心設計

- **`Task` + `CancellationToken` 協作式取消**：不強制終止執行緒。
  `job.Cancel()` 對執行中任務發取消訊號後立即返回；
  handler 觀察 token（傳進 `await` 或檢查 `IsCancellationRequested`）即提早收尾。
- **原子旗標防重疊**：上次還沒跑完，本次直接跳過。
- **精準對齊分鐘**：`System.Threading.Timer` 單發，每次對齊「下一分鐘 + 250ms」，支援 NTP 時鐘回調自我校正。
- **`Stop()` 發出訊號即返、不等待**：停 timer → 取消 daemon token → 逐一 `Cancel()`。
- **嚴格且彈性的解析**：支援標準 `*`、`?`、`*/n`、`start/n`、`a-b`、`a-b/n`、`a,b,c`；提供 `TryParse` 與靜態預檢。

## 2. 安裝

```bash
# 方案 A：原始碼引用（本倉庫）
dotnet add reference ../CronNETStd/CronNET.csproj

# 方案 B：NuGet（建置時自動產生）
dotnet add package CronNETStd --version 2.2026.0924.1
```

## 3. 快速開始

```csharp
using System;
using System.Threading;
using System.Threading.Tasks;
using CronNET;

using (var daemon = new CronDaemon())
{
    // (a) 最簡單：簡易任務（不可中途取消，只能阻止下次）
    daemon.AddJob("* * * * *", (ThreadStart)(() => Console.WriteLine("每分鐘")));

    // (b) 可取消的同步任務（推薦給 IO/迴圈工作）
    daemon.AddJob("*/5 * * * *", (CancellationToken ct) =>
    {
        for (int i = 0; i < 10; i++)
        {
            ct.ThrowIfCancellationRequested();   // ← 配合取消的關鍵
            Console.WriteLine($"step {i}");
            Thread.Sleep(500);
        }
    });

    // (c) 非同步任務（最推薦）
    daemon.AddJob("0 8 * * *", async (CancellationToken ct) =>
    {
        await Task.Delay(1000, ct);              // 把 ct 傳進 await
        Console.WriteLine("每天 08:00");
    });

    // 任務失敗不會拖垮排程，統一在這裡收（e.Job 取得出錯的任務實例）
    daemon.JobFailed += (s, e) =>
        Console.WriteLine($"[{e.FailedAt}] {e.ScheduleExpression} 失敗：{e.Exception.Message}");

    daemon.Start();
    Console.WriteLine("Enter 結束…"); Console.ReadLine();
    daemon.Stop(); // 發出取消訊號後立即返回，不阻塞
}
```

需要「發訊號＋等待收尾」（例如關機流程）時：

```csharp
// 最多等 10 秒，回傳 bool 表示是否全數正常結束（亦支援 Timeout.InfiniteTimeSpan 無限等待）
bool allCompleted = await daemon.StopAsync(TimeSpan.FromSeconds(10));
```

## 4. Cron 格式

```
┌─── 分（0-59）
│ ┌─── 時（0-23）
│ │ ┌─── 日（1-31）
│ │ │ ┌─── 月（1-12）
│ │ │ │ ┌─── 週（0-6，7 = 週日別名）
│ │ │ │ │
* * * * *
```

每欄支援：`*`、`?`、`*/n`、`start/n`（如 `0/15`）、`a-b`、`a-b/n`、`a,b,c`，以及逗號聯集（如 `1,2-10/2`）。
**日與週為 AND 語意**（兩者皆命中才執行；標準 cron 多為 OR，此處採用 AND）。

### 範例一覽

| 需求 | 寫法 |
|---|---|
| 每分鐘 | `* * * * *` |
| 每 2 分鐘 | `*/2 * * * *` |
| 每 5 分鐘 | `*/5 * * * *` |
| 每 15 分鐘（0、15、30、45 分） | `0/15 * * * *` 或 `*/15 * * * *` |
| 每小時整點 | `0 * * * *` |
| 每小時的 0、1、2 分 | `0,1,2 * * * *` |
| 每小時第 1～55 分鐘每分鐘 | `1-55 * * * *` |
| 每天 1、10、20 點的每分鐘 | `* 1,10,20 * * *` |
| 每天 08:00 | `0 8 * * *` |
| 每天 12:30 | `30 12 * * *` |
| 每天 08:00 與 20:00 | `0 8,20 * * *` |
| 每晚 22～23 點每 15 分 | `*/15 22-23 * * *` |
| 每週一 09:00（週一 = 1） | `0 9 * * 1` |
| 每週一～五 09:00（工作日） | `0 9 * * 1-5` |
| 每月 1 號 00:00 | `0 0 1 * *` |
| 每月 1 號與 15 號 00:00 | `0 0 1,15 * *` |
| 每年 1/1 00:00 | `0 0 1 1 *` |
| 混合：分鐘 1、4、6、8、10 | `1,2-10/2 * * * *` |
| 週日 00:00（0 與 7 等價） | `0 0 * * 0` 或 `0 0 * * 7` |

### 驗證與預算下次時間

```csharp
// (1) 靜態驗證與安全解析（不拋例外）
if (CronSchedule.TryParse("0/15 * * * *", out var schedule))
{
    Console.WriteLine("排程有效");
    
    // (2) 預算下次觸發時間點
    DateTime? nextRun = schedule.GetNextOccurrence(DateTime.Now);
    Console.WriteLine($"下次執行時間：{nextRun}");
}

// (3) 靜態檢查格式
bool valid = CronSchedule.IsValidExpression("60 * * * *"); // false（超出範圍）
```

## 5. 行為細節

1. **分鐘精度**：排程在「每分鐘 00 秒過後約 250ms」觸發一次；同一分鐘最多觸發一次。
2. **重疊跳過**：某 job 上次還沒跑完，本次直接跳過（`IsRunning` 可查詢）。
3. **取消是協作式**：`Cancel()` / `Stop()` 只發訊號。handler 必須觀察 `CancellationToken`
   （傳給 `Task.Delay` / HttpClient 等，或檢查 `ct.IsCancellationRequested`），否則該次會跑完；
    不接受 token 的委派（`ThreadStart`）只能阻止「下次」執行。
4. **例外隔離**：job 內拋例外不會殺掉排程，會經 `Failed` / `daemon.JobFailed` 事件送出。
5. **`Stop()` 不等待**：一律毫秒級返回；要等請用 `await StopAsync(timeout)`。
6. **`Start()` 冪等**：重複呼叫無害；`using` / `Dispose()` 會自動 `Stop()`。

## 6. API 一覽

- `CronSchedule(expression)` / `IsTime(DateTime)` / `GetNextOccurrence(DateTime)` /
  `Minutes/Hours/DaysOfMonth/Months/DaysOfWeek`（快取唯讀）、`Expression` /
  `CronSchedule.TryParse(expr, out schedule)` / `CronSchedule.IsValidExpression(expr)`
- `CronJob(schedule, handler)`：handler 可為 `ThreadStart` / `Action` /
  `Action<CancellationToken>` / `Func<CancellationToken, Task>`；
  `ExecuteAsync(now, daemonToken)` / `Cancel()` / `IsRunning` / `CurrentTask` / `Failed`
- `CronDaemon`：`AddJob` ×4（支援自訂 `ICronJob`）、`RemoveJob`、`Clear`、`Start`、`Stop`、
  `StopAsync(timeout)`（回傳 `Task<bool>`）、`IsRunning`、`Jobs`、`JobFailed`、`Dispose`
